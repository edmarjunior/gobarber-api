## YARN
	# baixar (site atual: https://classic.yarnpkg.com/en/) e instalar o yarn como gerenciador de pacotes
	# yarn (para baixar as dependencias)
	# yarn dev (para iniciar a aplicação com nodemon)

## DOCKER
	# docker ps -a (listar todos containers)
	# docker ps (lista somente os containers que estão sendo executados)
	# docker run --name database -e POSTGRES_PASSWORD=docker -p 5432:54322 -d -t postgres (criar um container contendo 1 imagem do postgres)
	# docker stop database (parando o container executado acima)
	# docker start database (inicinando o container)
	# docker container rm 70155288734a (remove o container com o id 70155288734a)


## SEQUELIZE
	# yarn sequelize migration:create --name=create-files (criar migration para tabela de arquivos)
	# yarn sequelize db:migrate (rodar as migrations na base de dados)


## DATE-FNS
	# yarn add date-fns@next (para instalar biblioteca para trabalhar com datas, obs: o @next é só pra pegar a versão mais recente)


## MONGODB
	# docker run --name mongobarber -p 27017:27017 -d -t mongo (criar container com o mongoDb)
	# yarn add mongoose (instalar ORM para trabalhar com o mongo)


## NODEMAILER (para envio de e-mail)
	# yarn add nodemailer


## HANDLEBARS (para criação de templates de e-mail)
	# yarn add express-handlebars nodemailer-express-handlebars (integração do handlebars com o express, e integração do nodemailers com o express-handlebars)

## REDIS
	# docker run --name redisbarber -p 6379:6379 -d -t redis:alpine
	# yarn add bee-queue (para criar filas)
	# yarn queue (script criado no package.json para processar as filas)

## SENTRY (para monitorar erros em ambiente de produção)
	# yarn add @sentry/node@5.13.0
	# yarn add express-async-errors (para o express capturar erros em métodos async)
	# yarn add youch (para retornar json de internal server error)

## Environment (.env)
	# yarn add dotenv (para utilizar a variavel global process do node importando no app.js)

## Jest (para testes)
	# yarn add jest -D (instalação somente em ambiente de desenvolvimento)
	# yarn jest --init
	# yarn add --dev @sucrase/jest-plugin (para utilizar o import/export)
	# yarn add -D @types/jest (para utilizar intelisense)
	# yarn test (executa os testes)
	# yarn add sqlite3 -D (para utilizar o sqlite)
	# yarn add supertest -D (para fazer requisições nos testes de integração)
	# yarn add factory-girl faker -D (para criar factories e dados fictícios)


# Cache
	# yarn add ioredis (cliente para o redis)

# Segurança
	# yarn add express-brute express-brute-redis (o primeiro para limitar a quantidade de requisições em uma rota, e o segundo para salvar do redis as informações de limitação)
	# yarn add helmet (adiciona headers de segurança no response daa requisições)
	# yarn add express-rate-limit redis rate-limit-redis (express-rate-limit: para limitar a quantidade de requisições em todas as rotas. redis: para passar uma conexão para o express-rate-limit. rate-limit-redis: para configurar a conexão)
	# yarn add cors (para definir quais front-end podem acessar a api)

# Segurança no repositório do github
	# habilitar "Allow GitHub to perform read-only analysis of this repository" e "Security alerts" em Settings > Data services
	# habilitar "Automated security updates" em Security

## TODO:
	# Bug - Quando dispara exception nos validators em vez de voltar erro 400 volta 500 porque cai no exceptionHandler do app.js
	# Melhoria - utilizar o greenkeeper para automatizar as atualizações de dependencias do package.json
	# Criar um dominio para a aplicação e configurar na digital ocean na seção Networking
